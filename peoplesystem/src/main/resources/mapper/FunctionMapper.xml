<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.tlm.people.dao.FunctionDao">

    <!-- 查询所有学生 -->
    <select id="findAll" resultType="com.tlm.people.entity.User">
        select
            id,username,password,del_flag,createtime,updatetime
        from
            user
    </select>
</mapper>